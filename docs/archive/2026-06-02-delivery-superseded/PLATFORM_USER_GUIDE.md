# Team AI Platform 用户手册（真实可用版，2026-05-20）

本手册只描述当前版本“实际能用”的能力。

## 一、先看结论

当前平台是“可运行的治理控制台 + 部分业务功能可用”，不是完整低代码业务平台。

- 已可用：登录、团队/项目权限链路、知识文档（文本）管理、技能管理、智能体基础管理、治理中心动作触发、Git Hook 事件观测。
- 部分可用：RAG/Skill/Agent 演进动作（需管理员权限和前置数据）。
- 暂不可用：可视化拖拽流程编排、知识文件上传按钮、完整观测指标体系。

## 二、你能在哪些页面做什么

### 1) 知识库（RAG）页

- 页面入口：左侧“知识库(RAG)”。
- 当前能力：新建文本知识文档、搜索、删除。
- 当前限制：没有“上传文件”按钮，只有文本录入。
- 前置条件：必须先有 Project，否则无法创建文档。

### 2) 智能体页

- 页面入口：左侧“智能体”。
- 当前能力：新建、删除、查看基础信息。
- 当前限制：创建时必须绑定 Project；如果当前用户不在该 Project 所属 Team，会报权限错误。

### 3) 治理中心页

- 页面入口：左侧“治理中心”。
- 当前能力：
	- 上传 Skill Bundle（逻辑上传，不是文件选择器）
	- 生成/应用 Team Rules
	- 会话知识入 RAG
	- RAG 总结到 Skill
	- 生成/优化 Agent Workflow（治理动作，不是拖拽编排 UI）
- 当前限制：多数动作需要管理员能力或正确 Team ID/Skill ID/Rule Set。

### 4) 观测中心页

- 页面入口：左侧“观测中心”。
- 当前能力：Git Hook 事件最完整；插件日志/审计日志/会话日志可看，但覆盖面有限。
- 当前限制：不是完整 APM/Tracing 监控系统，很多业务指标尚未沉淀。

### 5) 设置页与用户管理

- 页面入口：左侧“系统设置”。
- 当前能力：个人设置、运行时配置、管理员用户管理（在设置页的“用户管理”标签）。
- 说明：“用户管理菜单”与“设置里的用户管理”本质是同一能力入口（路由会归并到设置页）。

## 三、最短可用路径（按顺序）

1. 登录平台。
2. 在治理中心先创建 Team（如果尚未创建）。
3. 创建 Project 并确保当前账号是该 Team 成员。
4. 到“知识库(RAG)”新建文本知识文档。
5. 到“技能平台”创建技能。
6. 到“智能体”创建智能体（绑定已有 Project）。
7. 需要治理演进时，再去“治理中心”执行 Skill/RAG/Workflow 动作。

如果第 4 步或第 6 步失败，优先检查 Team/Project 成员关系。

## 四、为什么 LiteLLM 里看不到 Skill/MCP/RAG/Agent

这是架构分层，不是你使用错误：

- LiteLLM：模型网关（模型路由、调用、密钥）。
- Skill/RAG/Agent/MCP：平台控制面数据与能力，不在 LiteLLM 控制台原生展示。

因此你在 LiteLLM 面板看不到这些对象是正常现象。当前版本“互通”主要体现在调用链路，而不是同一个后台界面。

## 五、当前版本边界（避免误解）

- 没有知识文件上传控件（仅文本新建）。
- 没有可视化拖拽流程编排器。
- 观测中心不是全链路监控，当前以事件与审计记录为主。
- Team/Project 权限是强校验，未加入团队就无法新增相应资源。

## 六、常见报错和处理

- 报错“Team not found”：先创建 Team。
- 报错“Not in this team”：把当前用户加入 Team。
- 报错“Project not found”：先创建 Project，再创建知识/智能体。
- 报错“Not allowed to access project”：当前用户不在该 Project 对应 Team。
- 治理中心动作失败：确认是否管理员、Team ID/Skill ID 是否存在。

## 七、版本建议

如果你要的是“完全可视化可配置化”，当前版本还不够。
建议把后续迭代聚焦到三项：

1. 知识库文件上传与批量入库。
2. 业务流程可视化编排器（拖拽）。
3. 观测中心从日志视图升级到完整指标与链路观测。